# Copia este archivo como .env.local y rellena los valores
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# Vercel Postgres (Neon) — auto-inyectada por Vercel al conectar la DB
POSTGRES_URL=

# Secret para proteger el endpoint /api/sync (string aleatorio)
CRON_SECRET=
